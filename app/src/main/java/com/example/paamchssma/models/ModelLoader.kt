package com.example.paamchssma.models

import android.content.Context

/**
 * ModelLoader - DISABLED
 * 3D model loading is handled in MainActivityFull.kt via Three.js
 */
class ModelLoader(private val context: Context) {
    suspend fun loadFacultyModel(): Boolean {
        return true
    }
}
